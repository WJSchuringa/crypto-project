name: unit-tests
on: 
  push:
    branches:    
      - '**'        # matches every branch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Unit Tests
        run: go test